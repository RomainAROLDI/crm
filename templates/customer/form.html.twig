<div class="container">
        <h2 class="text-center mt-5 mb-3">{{ title }}</h2>
        <div class="card">
            <div class="card-body">
                {{ form_start(form) }}
                    <div class="form-group">
                        {{ form_row(form.lastName, {'label': 'Nom', 'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.firstName, {'label': 'Prénom', 'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.email, {'label': 'Mail', 'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.company, {'label': 'Entreprise', 'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.job, {'label': 'Poste', 'attr': {'class': 'form-control'}}) }}
                    </div>

                    <div class="d-flex flex-row justify-content-between">
                        <button class="btn btn-primary mt-3">{{ bouton_libelle }}</button>
                        <button class="btn btn-secondary d-flex mt-3" href="{{ path('app_customer') }}">Annuler</button>
                    </div>
                    
                {{ form_end(form) }}
            </div>
        </div>
    </div>